{
  "name": "hdstradingopc-website",
  "private": true,
  "version": "1.0.0",
  "description": "Official website for HDS Trading OPC \u2014 hdstradingopc.com",
  "homepage": "https://hdstradingopc.com",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c magenta,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run lint --workspace client && npm run build --workspace client && npm run build --workspace server",
    "build:webspace": "npm run lint --workspace client && npm run build --workspace client && node scripts/generate-static-seo.mjs && cp deploy/webspace/.htaccess client/dist/.htaccess",
    "start": "npm run start --workspace server",
    "seed": "npm run seed --workspace server",
    "lint": "npm run lint --workspace client && npm run lint --workspace server",
    "clean": "rm -rf client/dist server/dist node_modules/.cache"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
